version: '3.8'

# NOTE: MySQL is now running locally - not in Docker
# This compose file is kept for reference or future use
# You can still run other services here if needed

services:
  # MySQL service has been removed - using local MySQL installation instead
  # Connect to: localhost:3306
  # Username: root
  # Password: root123
  # Database: sims_db
  
  # Uncomment below if you want to use Docker MySQL again
  # mysql:
  #   image: mysql:5.7
  #   container_name: sims-mysql
  #   restart: unless-stopped
  #   environment:
  #     MYSQL_ROOT_PASSWORD: Admin123@
  #     MYSQL_DATABASE: sims_db
  #   ports:
  #     - '3306:3306'
  #   volumes:
  #     - mysql_data:/var/lib/mysql
  #     - ./scripts/init.sql:/docker-entrypoint-initdb.d/init.sql
  #   networks:
  #     - sims-network
  #   healthcheck:
  #     test: ['CMD', 'mysqladmin', 'ping', '-h', 'localhost']
  #     timeout: 5s
  #     retries: 10

  # PHPMyAdmin (optional - uncomment if you want to use it)
  # phpmyadmin:
  #   image: phpmyadmin:latest
  #   container_name: sims-phpmyadmin
  #   restart: unless-stopped
  #   environment:
  #     PMA_HOST: 127.0.0.1
  #     PMA_PORT: 3306
  #     PMA_USER: root
  #     PMA_PASSWORD: root123
  #   ports:
  #     - '8080:80'
  #   networks:
  #     - sims-network

volumes:
  mysql_data:
    driver: local

networks:
  sims-network:
    driver: bridge
